public class Grid {
  List<Tile> grid;

  public Grid() {
    grid = new ArrayList<Tile>(16)
  }

  public getTile(int a, int b) {
    if ((a > 0 and a < 5) or (b > 0 and b < 5)) {
      
    }
  }
}
